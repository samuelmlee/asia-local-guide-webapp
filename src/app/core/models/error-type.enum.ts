export enum ErrorType {
  NETWORK = 'network',
  UNAUTHORIZED = 'unauthorized',
  FORBIDDEN = 'forbidden',
  NOT_FOUND = 'notFound',
  VALIDATION = 'validation',
  SERVER = 'server',
  UNKNOWN = 'unknown',
}
